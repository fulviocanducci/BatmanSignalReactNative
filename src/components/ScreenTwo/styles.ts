import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  appContainer: {
    marginTop: 45,
  },
  appImageContainer: {
    textAlign: "left",
    width: "100%",
  },
  appImage: {
    resizeMode: "contain",
    height: 100,
  },
});
